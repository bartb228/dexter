---
phase: 02-universe-selection
plan: 01
type: execute
domain: stock-scanner (Python) + dexter (TypeScript)
---

<objective>
Let `run_quality_screen` target the right universe. Add a fast, curated static
`quality_growth` universe to the scanner, and expose a validated `universe` argument on the
tool so it can screen sp500 / russell_1000 / russell_3000 / dow_30 / quality_growth instead
of only the mature default 40-name list.

Purpose: The default universe is mature/leveraged/slow-growth and structurally fails a
quality-growth screen. This makes the tool point at names that can actually pass (proven:
LRCX, MNST, DECK, RMD all cleared the identical gates on a growth set).
Output: `universes.quality_growth()` + scanner wiring; `universe` param on the tool; tests.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-failure-observability/01-02-SUMMARY.md

# Source files:
@src/tools/finance/quality-screen.ts   # this repo — QualityScreenInputSchema, args build
# /Users/Ambartsum/code/Stock scanner/scanner/universes.py  — sp500()/russell_1000()/russell_3000()/dow_30()/custom(), each -> List[str]
# /Users/Ambartsum/code/Stock scanner/scanner/scanner.py    — argparse --universe choices (~1414), resolve elif chain (~1554-1571)

**Established patterns:** universes.py functions return `List[str]`; sp500/russell scrape
Wikipedia (network), dow_30 too. scanner.py resolves `args.universe` via a literal elif
chain and validates via argparse `choices=[...]`. The tool builds an args array and spawns
with no shell; tickers are charset-validated.
**Constraining decisions:** D5 (quality_growth is a STATIC hardcoded list — no network,
keeps the tool fast/offline). D6 (tool never throws). `--symbols` beats `--universe` in the
scanner; the tool must not send both.
**Prereq:** Phase 01 shipped (failure_summary makes a broad-universe scan far more useful).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add curated static quality_growth() universe to the scanner</name>
  <files>/Users/Ambartsum/code/Stock scanner/scanner/universes.py, /Users/Ambartsum/code/Stock scanner/scanner/scanner.py, /Users/Ambartsum/code/Stock scanner/scanner/tests/test_universes.py</files>
  <action>
  In universes.py add `def quality_growth() -> List[str]:` returning a hardcoded, de-duped,
  sorted list of ~40 net-cash / high-ROIC compounder candidates diversified across sectors
  (these are CANDIDATES the screen then filters — not pre-vetted passers). Seed with the
  proven passers + strong candidates, e.g.: LRCX MNST DECK RMD KLAC AMAT SNPS CDNS ANET
  MPWR MCHP IDXX VEEV ELF POOL FAST PAYX MSFT GOOGL META ADBE ACN INTU NOW ISRG ALGN WST
  DXCM ODFL FICO MSCI VRSK ROL CTAS TXN AVGO NKE LULU TSCO. No network call — return the
  literal list (optionally `sorted(set(...))` for determinism). Add a module docstring line
  noting it is a static curated seed, not an index membership list.

  Wire scanner.py: add "quality_growth" to the argparse `choices=[...]` (~line 1414) and an
  `elif args.universe == "quality_growth": symbols = universes.quality_growth()` branch in
  the resolve chain (~1557-1565), placed alongside the other elifs.

  Add tests/test_universes.py::test_quality_growth_is_static_nonempty: returns a non-empty
  list of >30 uppercase tickers, is deterministic across two calls (==), makes NO network
  call (monkeypatch requests.get to raise, assert it still returns — proving no fetch), and
  contains the four proven passers LRCX/MNST/DECK/RMD.
  </action>
  <verify>cd "/Users/Ambartsum/code/Stock scanner/scanner" && python3 scanner.py --help | grep quality_growth && python3 -c "import universes; xs=universes.quality_growth(); print(len(xs)); assert xs==universes.quality_growth() and {'LRCX','MNST','DECK','RMD'} <= set(xs)" && python3 -m pytest tests/test_universes.py -q</verify>
  <done>quality_growth in --help choices; returns a deterministic static list ≥30 incl. the 4 passers; no network; test green.</done>
</task>

<task type="auto">
  <name>Task 2: Add validated `universe` param to run_quality_screen (Dexter)</name>
  <files>src/tools/finance/quality-screen.ts</files>
  <action>
  Add to QualityScreenInputSchema: `universe: z.enum(['default','sp500','russell_1000',
  'russell_3000','dow_30','quality_growth']).optional().describe('Which universe to screen
  when `symbols` is omitted. "quality_growth" = fast curated compounder shortlist
  (recommended). "default" = the mature large-cap list (rarely passes this screen). sp500/
  russell_* scrape a live index and are SLOW (minutes).')`.

  In func(): after validating symbols, decide the universe wiring:
  - If `symbols.length` > 0 → pass `--symbols …` as today; IGNORE `universe` (scanner: symbols
    win). Do not send both.
  - Else if `input.universe` and it !== 'default' → push `'--universe', input.universe`.
  - Else → no universe flag (scanner defaults to `custom` = config.UNIVERSE), unchanged.
  Keep the enum as the ONLY allowed values (z.enum already rejects arbitrary strings — this
  is the injection guard for the flag; no raw user string reaches argv). Update `screened:`
  in the result to report the chosen universe label when symbols are absent.

  Update RUN_QUALITY_SCREEN_DESCRIPTION "## Notes": document `universe`, recommend
  `quality_growth` for finding passers, and warn sp500/russell_* are slow (may approach the
  300s timeout). Cross-reference the Phase-01 note ("widen the universe rather than loosening
  reasoning").
  </action>
  <verify>cd /Users/Ambartsum/code/Dexter && bun run build 2>&1 | tail -5   (compiles; enum rejects bad values at parse time)</verify>
  <done>Schema accepts `universe`; symbols-present ignores it; universe!=default sends `--universe`; description documents it; build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Test the universe wiring + end-to-end smoke</name>
  <files>src/tools/finance/quality-screen.test.ts</files>
  <action>
  Extend quality-screen.test.ts. To keep it unit-level (no real spawn), refactor the argv
  assembly in quality-screen.ts into a small pure exported helper
  `buildScanArgs({profile, outPath, rejPath, top, symbols, universe})` and test IT:
  1. symbols present → args include '--symbols' + the tickers, and NO '--universe'.
  2. universe='quality_growth', no symbols → args include '--universe','quality_growth'.
  3. universe='default', no symbols → NO '--universe' flag (falls through to scanner default).
  4. both symbols AND universe given → symbols win, '--universe' absent.
  5. args always include '--json' outPath and '--rejections-json' rejPath (Phase-01 contract).
  (Have func() call buildScanArgs so the tool and the test share one code path.)

  Then a documented END-TO-END smoke (manual, not in the bun suite — spawns Python):
  `run_quality_screen({ universe: 'quality_growth', top: 10 })` returns passed ≥ 1 with real
  picks (LRCX/DECK/MNST/RMD class) AND a failure_summary for the rejected remainder.
  </action>
  <verify>cd /Users/Ambartsum/code/Dexter && bun test src/tools/finance/quality-screen.test.ts 2>&1 | tail -15</verify>
  <done>All 5 buildScanArgs cases pass; func() uses the same helper; smoke documented in the SUMMARY.</done>
</task>

</tasks>

<verification>

**Validation pattern (4-reviewer close-out, parallel via Agent tool, run_in_background):**
1. spec-compliance (`feature-dev:code-reviewer`) — all 5 argv cases + universes test; done-criteria PASS/FAIL.
2. code-quality (`feature-dev:code-reviewer`) — symbols-vs-universe precedence exactly matches scanner semantics; enum is the injection guard; no shell.
3. silent-failure-hunter (`pr-review-toolkit:silent-failure-hunter`) — an unknown universe is rejected at schema parse (not silently defaulted); quality_growth network-free claim holds.
4. test-thoroughness (`pr-review-toolkit:pr-test-analyzer`) — both-given precedence case, default-fallthrough case, Phase-01 flag-still-present case.

Conditional lenses:
- [x] type-design-analyzer does NOT apply (no new types; z.enum literal union only).
- [x] comment-analyzer does NOT apply.

Before declaring plan complete:
- [ ] `python3 -m pytest tests/test_universes.py -q` green; full scanner suite still green
- [ ] `bun test src/tools/finance/quality-screen.test.ts` all pass
- [ ] `bun run build` clean
- [ ] Smoke: `universe: 'quality_growth'` returns passers + a failure_summary
</verification>

<success_criteria>
- Curated static quality_growth universe exists in the scanner and is selectable end-to-end.
- run_quality_screen exposes a validated `universe` arg with correct symbols-precedence.
- buildScanArgs unit-tested (5 cases); scanner + Dexter suites green; build clean.
- **Phase 02 complete** → whole project Definition of Done met (observability + universe).
</success_criteria>

<output>
After completion, create `.planning/phases/02-universe-selection/02-01-SUMMARY.md`
(curated list contents, precedence rule, smoke result). Mark Phase 02 + project complete in STATE.md.
</output>
