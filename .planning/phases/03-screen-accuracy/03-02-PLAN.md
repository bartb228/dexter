---
phase: 03-screen-accuracy
plan: 02
type: execute
domain: stock-scanner (Python) + dexter (TypeScript)
---

<objective>
Plan A: augment `passes_screen` to emit a structured `GateResult{gate, value, threshold,
passed}` at each gate site ALONGSIDE its existing (byte-identical) failure string. Derive
the rejection tally, a ranked `near_miss` watchlist, and named (not "Other") gate keys from
these structured records — retiring the 01-01 head-token string parser.

Purpose: A rejected name is currently indistinguishable from one that failed everything, and
non-canonical gates hide in "Other". Structured results make the screen fully self-explaining
and give near-miss its numeric distance-to-threshold.
Output: GateResult emission; structured summarize_rejections; near_miss on the tool result.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@docs/plans/2026-07-18-quality-screen-v2-design.md
@.planning/phases/01-failure-observability/01-01-SUMMARY.md

# Source files:
# scoring.py — passes_screen (~1395-1979, the ~40 failures.append sites), summarize_rejections (~1982+)
# scanner.py — run() rejection capture (result is None branch ~995), context["rejections"]
# report.py — save_rejections_json
@src/tools/finance/quality-screen.ts — normalizeFailureSummary, FailureSummary, RUN_QUALITY_SCREEN_DESCRIPTION

**Constraining decisions (design):** Plan A = ADDITIVE, in-place. Do NOT rewrite the failure
strings — append a GateResult next to each existing string (strings byte-identical, pinned
by test). NO return-arity change to passes_screen: bubble GateResults via the mutable `f`
dict (`f["gate_results"] = [...]`), the same pattern passes_screen already uses for
proxy_flags/upside_pct. Third state `passed=None` = skipped/data-unavailable (replaces
today's silent skip). D6 (tool never throws) preserved on the Dexter side.
</context>

<tasks>

<task type="auto">
  <name>Task 1: GateResult + emit alongside byte-identical strings</name>
  <files>/Users/Ambartsum/code/Stock scanner/scanner/scoring.py, /Users/Ambartsum/code/Stock scanner/scanner/tests/test_gate_results.py</files>
  <action>
  Add a `GateResult = NamedTuple('GateResult', gate:str, value:Optional[float],
  threshold:Optional[float], passed:Optional[bool])`. In passes_screen, accumulate a local
  `gate_results: List[GateResult]` and at EACH gate site append a record (passed=False where a
  failure string is appended; passed=True on the clear path; passed=None where the gate is
  skipped for missing data). Set `f["gate_results"] = gate_results` before the return. Do NOT
  touch the failure-string appends. `gate` uses the canonical name (ROIC, Debt/Eq, …); for
  currently-"Other" gates (Mscore, GrossMargin, RuleOf40, RuleOfXUpside, AvgDailyVol, Upside,
  EPSGrowth, op-lev, net-dilution) use their real names.

  TEST tests/test_gate_results.py: (1) a fixture that fails ROIC+Debt/Eq yields GateResults
  with passed=False for those and the numeric value/threshold; (2) every failure string still
  present + BYTE-IDENTICAL (compare passes_screen failures before/after via a golden fixture);
  (3) a skipped gate (missing data) yields passed=None, no failure string; (4) each False
  GateResult's (gate,value,threshold) is consistent with its failure string (no within-fn drift).
  </action>
  <verify>cd "/Users/Ambartsum/code/Stock scanner/scanner" && python3 -m pytest tests/test_gate_results.py -q && python3 -m pytest -q -p no:cacheprovider 2>&1 | tail -3</verify>
  <done>GateResults emitted via f; failure strings byte-identical (pinned); full suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Structured summarize_rejections + ranked near_miss + named tally</name>
  <files>/Users/Ambartsum/code/Stock scanner/scanner/scoring.py, /Users/Ambartsum/code/Stock scanner/scanner/scanner.py, /Users/Ambartsum/code/Stock scanner/scanner/report.py, /Users/Ambartsum/code/Stock scanner/scanner/tests/test_summarize_rejections.py</files>
  <action>
  Bubble gate_results with each rejection: in run()'s capture, record (symbol, failures,
  gate_results) (extend the tuple / use a small dict). Rebuild summarize_rejections on the
  STRUCTURED data: gate_tally counts by GateResult.gate (passed=False), so Mscore/GrossMargin
  become real keys (no head-token parsing, no "Other" except genuinely unknown). Add
  `near_miss`: for each rejected symbol compute (n_failed = #False gates, margin = Σ relative
  distance of each failed gate from its threshold); return the rejected names sorted by
  (n_failed, margin) with their failing gates+values, capped (e.g. 15). Keep `samples` for
  back-compat. Retire the _REJECTION_GATE_BY_HEAD map (or keep only as a fallback for
  string-only inputs). Update save_rejections_json + extend test_summarize_rejections.py
  (structured input; near_miss ordering; named tally; infra-skips still excluded).
  </action>
  <verify>cd "/Users/Ambartsum/code/Stock scanner/scanner" && python3 -m pytest tests/test_summarize_rejections.py tests/test_gate_results.py -q && python3 scanner.py --profile quality_moat --universe quality_growth --rejections-json /tmp/nm.json --no-rss-poll >/dev/null 2>&1; python3 -c "import json;s=json.load(open('/tmp/nm.json'));print('near_miss[0]',s['near_miss'][0]);print('tally',s['gate_tally'])"</verify>
  <done>gate_tally keyed by real gate names (no Other for known gates); near_miss ranked (fewest gates × smallest margin); suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Surface near_miss through run_quality_screen (Dexter)</name>
  <files>src/tools/finance/quality-screen.ts, src/tools/finance/quality-screen.test.ts</files>
  <action>
  Extend FailureSummary + normalizeFailureSummary to carry `near_miss: Array<{symbol,
  n_failed, gates: Array<{gate, value, threshold}>}>` (defensive coercion; cap; never throw —
  D6). Attach it to the tool result. Update RUN_QUALITY_SCREEN_DESCRIPTION: instruct the model
  to cite near_miss ("names one gate away, e.g. GOOGL — ROIC 14.3% vs 15%") when passers are
  few. Add unit tests for the near_miss normalization (non-object/array/missing → safe).
  </action>
  <verify>cd /Users/Ambartsum/code/Dexter && bun test src/tools/finance/quality-screen.test.ts 2>&1 | tail -4 && bun run typecheck 2>&1 | tail -2</verify>
  <done>failure_summary carries a normalized near_miss; description cites it; tests + typecheck green.</done>
</task>

</tasks>

<verification>
**4-reviewer close-out (Workflow, parallel, adversarially verified):**
1. spec-compliance — GateResults at every site; strings byte-identical; near_miss ranked correctly.
2. code-quality+security — no passes_screen arity change (bubble via f); no drift between string+struct.
3. silent-failure-hunter — passed=None skip is not counted as a fail; infra-skips still excluded; tool never throws on near_miss.
4. test-thoroughness — byte-identical golden test; near_miss ordering; named tally; skip state.

Conditional: type-design-analyzer APPLIES (GateResult NamedTuple + near_miss shape) — run it.
comment-analyzer N/A.

Before complete:
- [ ] failure strings byte-identical (golden test); full scanner suite green
- [ ] near_miss ranks GOOGL-class (1 gate, small margin) first on a real scan
- [ ] bun test + typecheck green
</verification>

<success_criteria>
- passes_screen emits structured GateResults; failure strings byte-identical; single source of truth.
- gate_tally keyed by real gate names; ranked near_miss watchlist on the tool result.
- 01-01 head-token parser retired; full suite green; tool never throws.
</success_criteria>

<output>
Create `.planning/phases/03-screen-accuracy/03-02-SUMMARY.md` (GateResult shape, near_miss
ranking rule, byte-identical proof, tally-by-name). Next: 03-03.
</output>
