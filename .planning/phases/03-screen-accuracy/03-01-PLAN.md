---
phase: 03-screen-accuracy
plan: 01
type: execute
domain: stock-scanner (Python) + dexter (TypeScript)
---

<objective>
Add a cash-adjusted "operating" ROIC alongside the existing book ROIC so cash-rich
compounders are no longer penalized for holding cash. The quality_moat ROIC gate passes
if EITHER book or operating ROIC clears 15%; both are reported.

Purpose: NVDA (book ROIC 12.6%) and GOOGL (14.3%) fail the screen ONLY on ROIC — a
measurement artifact of the book invested-capital denominator (equity + debt, no cash
netting). Operating ROIC nets out cash and is expected to flip both to passing.
Output: `compute_roic_operating()` + gate change + both values surfaced; NVDA/GOOGL pass.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/plans/2026-07-18-quality-screen-v2-design.md

# Source files:
# /Users/Ambartsum/code/Stock scanner/scanner/scoring.py — compute_roic (~432), the ROIC gate in passes_screen (~1558-1575)
# /Users/Ambartsum/code/Stock scanner/scanner/data_sources.py — the fundamentals dict (find the cash / short-term-investments field names)
# /Users/Ambartsum/code/Stock scanner/scanner/report.py — result record fields (surface roic_operating)
@src/tools/finance/quality-screen.ts — SELECT list (add roic_operating)

**Established patterns:** compute_roic returns Optional[float]; NOPAT = op_income × 0.79;
IC = equity × (1 + debt_eq). Skip-on-missing returns None (Plan 07-15 visibility).
**Constraining decisions (from the design):** ADDITIVE — do NOT change the book ROIC or
its skip semantics; add operating ROIC beside it. Gate = `max(book, operating) ≥ 15%`.
Deterministic subtract-ALL-cash rule (cash + short-term investments), no "excess cash"
judgment. Fail-closed strings preserved (the ROIC failure string only fires when BOTH miss).
</context>

<tasks>

<task type="auto">
  <name>Task 1: compute_roic_operating() (cash-adjusted IC) — TEST-FIRST</name>
  <files>/Users/Ambartsum/code/Stock scanner/scanner/scoring.py, /Users/Ambartsum/code/Stock scanner/scanner/data_sources.py, /Users/Ambartsum/code/Stock scanner/scanner/tests/test_compute_roic_operating.py</files>
  <action>
  FIRST grep data_sources.py + a get_fundamentals sample for the exact cash / short-term-
  investment field names (e.g. cash, cash_and_equivalents, short_term_investments) — do NOT
  guess. Then add `compute_roic_operating(facts) -> Optional[float]` mirroring compute_roic
  but with `IC = (equity + total_debt) − cash − short_term_investments`, floored so IC stays
  > 0 (if netting cash makes IC ≤ 0, fall back to book IC — a firm can't have negative
  invested capital). Same NOPAT, same bool/None guards, same skip-on-missing semantics.
  Missing cash fields → treat cash as 0 (operating == book), never crash.

  RED-first: tests/test_compute_roic_operating.py — (1) cash-heavy fixture: operating > book;
  (2) no-cash fixture: operating == book; (3) missing-cash-field: operating == book (cash=0);
  (4) cash so large IC would go ≤ 0: falls back to book IC (no div-by-zero / negative);
  (5) missing op_income/equity → None (skip semantics preserved).
  </action>
  <verify>cd "/Users/Ambartsum/code/Stock scanner/scanner" && python3 -m pytest tests/test_compute_roic_operating.py -q</verify>
  <done>compute_roic_operating exists; 5 cases pass; never negative/zero IC; skip semantics preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Gate on max(book, operating); surface both values</name>
  <files>/Users/Ambartsum/code/Stock scanner/scanner/scoring.py, /Users/Ambartsum/code/Stock scanner/scanner/scanner.py (or report.py — where the result dict is built), src/tools/finance/quality-screen.ts</files>
  <action>
  In passes_screen's ROIC gate (~1558-1575): compute both `roic` (book) and `roic_op`
  (operating). The gate FAILS only when BOTH are None/below floor. Preserve the EXACT
  existing failure strings for the book path; when book fails but operating passes, the gate
  PASSES and appends NO failure. When both fail, append the existing "ROIC=<book> < 15%"
  string (book value, unchanged) so 03-02/01-01 bucketing + fail-closed tests stay intact.
  Attach both to the enriched result row (`roic`, `roic_operating`) where analyze_symbol
  builds the record. Add `roic_operating` to Dexter's SELECT list in quality-screen.ts.
  </action>
  <verify>cd "/Users/Ambartsum/code/Stock scanner/scanner" && python3 -m pytest -q -p no:cacheprovider 2>&1 | tail -3  &&  cd /Users/Ambartsum/code/Dexter && bun run typecheck 2>&1 | tail -2</verify>
  <done>Gate passes on max(book, operating); both surfaced; full scanner suite green; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: Tighter ROIC inputs (real effective tax) + live acceptance</name>
  <files>/Users/Ambartsum/code/Stock scanner/scanner/scoring.py, /Users/Ambartsum/code/Stock scanner/scanner/tests/test_compute_roic_operating.py</files>
  <action>
  Replace the flat 21% NOPAT tax with the firm's effective rate when derivable from EDGAR
  (income_tax_expense / pretax_income), clamped to [0, 0.35]; fall back to 0.21 when either
  input is missing/non-finite (grep data_sources for the field names first). Apply to BOTH
  book + operating NOPAT. Add a test: a fixture with a known effective rate uses it; a
  fixture missing tax inputs falls back to 0.21 (byte-identical to today).

  LIVE ACCEPTANCE (documented in SUMMARY, not the bun suite): scanner --symbols NVDA GOOGL
  --profile quality_moat --rejections-json → BOTH now PASS (or their roic_operating ≥ 15%
  and they no longer appear in rejections).
  </action>
  <verify>cd "/Users/Ambartsum/code/Stock scanner/scanner" && python3 scanner.py --profile quality_moat --symbols NVDA GOOGL --json /tmp/ng2.json --rejections-json /tmp/ng2r.json --no-rss-poll --no-cache 2>&1 | grep -iE 'NVDA|GOOGL' | grep -iE 'PASSED|skipped'</verify>
  <done>Effective-tax path + fallback tested; live NVDA + GOOGL PASS quality_moat.</done>
</task>

</tasks>

<verification>
**4-reviewer close-out (Workflow, parallel, adversarially verified):**
1. spec-compliance (feature-dev:code-reviewer) — additive (book ROIC unchanged), gate = max, both surfaced, 5+ tests.
2. code-quality+security (feature-dev:code-reviewer) — IC never ≤ 0; effective-tax clamp; skip semantics; no arity change.
3. silent-failure-hunter — missing cash → cash=0 (not crash); both-None still fails closed with the exact string.
4. test-thoroughness (pr-test-analyzer) — cash-heavy/no-cash/missing/negative-IC/skip; effective-tax fallback.

Conditional: type-design N/A (floats, no new type). comment-analyzer N/A.

Before complete:
- [ ] tests/test_compute_roic_operating.py green; full scanner suite green (no regression on the 962)
- [ ] bun run typecheck clean
- [ ] LIVE: NVDA + GOOGL pass quality_moat
</verification>

<success_criteria>
- Operating (cash-adjusted) ROIC computed alongside book; gate = max(book, operating) ≥ 15%.
- Both values surfaced in picks; NVDA + GOOGL flip to passing (measurement artifact fixed).
- Book ROIC behavior + failure strings byte-identical; full suite green.
</success_criteria>

<output>
Create `.planning/phases/03-screen-accuracy/03-01-SUMMARY.md` (cash field names used, the
max(book,operating) rule, effective-tax handling, NVDA/GOOGL before→after). Next: 03-02.
</output>
