---
phase: 03-screen-accuracy
plan: 03
type: execute
domain: stock-scanner (Python) + dexter (TypeScript)
---

<objective>
Turn the structured gate results into a calibration tool — a `sensitivity` "what-if" report
("loosen ROIC to 12% → +N passers") — and fix `--top` so single-profile scans actually
truncate to the top-N picks by composite score.

Purpose: The aggregate tally shows which gate blocks the most names; sensitivity answers the
operator's real question — "which single gate, loosened how far, admits how many more?".
And `--top` is currently inert for single-profile mode (its help says "Ignored for
single-profile mode"), so run_quality_screen's `top` doesn't bound results.
Output: `sensitivity` in the rejections summary; `--top` truncation; both surfaced.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@docs/plans/2026-07-18-quality-screen-v2-design.md
@.planning/phases/03-screen-accuracy/03-02-PLAN.md

# Source files:
# scoring.py — summarize_rejections (now structured, from 03-02); gate_results
# scanner.py — run() single-profile path: df sort by composite (~1108), save_json (~1613); --top help (~1517 "Ignored for single-profile mode")
@src/tools/finance/quality-screen.ts — result payload

**Prereq:** 03-02 shipped (structured gate_results are the input to sensitivity).
**Constraining decisions:** sensitivity is derived ONLY from structured results (no re-scan).
`--top` truncation applies to the single-profile df BEFORE save_json, sorted by composite
desc (the sort already exists ~1108) — truncate `df.head(top_n)`. Do NOT change tier-mode.
</context>

<tasks>

<task type="auto">
  <name>Task 1: gate-sensitivity report from structured results</name>
  <files>/Users/Ambartsum/code/Stock scanner/scanner/scoring.py, /Users/Ambartsum/code/Stock scanner/scanner/tests/test_summarize_rejections.py</files>
  <action>
  Add a `sensitivity` block to the summarize_rejections output: for each gate, count the
  rejected names whose ONLY failing gate is that gate ("sole blockers") + the loosened
  threshold that would admit the closest K of them (from the GateResult values). Shape:
  {gate: {sole_blockers: int, would_admit_at: float|null, examples: [symbol,...]}}. Pure,
  deterministic, derived from the gate_results already captured — no re-scan. TEST: a
  constructed rejection set where 3 names fail ONLY ROIC (values 0.126/0.143/0.11) →
  sensitivity['ROIC'].sole_blockers == 3 and would_admit_at reflects the values.
  </action>
  <verify>cd "/Users/Ambartsum/code/Stock scanner/scanner" && python3 -m pytest tests/test_summarize_rejections.py -q</verify>
  <done>sensitivity reports per-gate sole-blockers + admit-threshold; deterministic; tested.</done>
</task>

<task type="auto">
  <name>Task 2: Fix --top truncation for single-profile mode</name>
  <files>/Users/Ambartsum/code/Stock scanner/scanner/scanner.py, /Users/Ambartsum/code/Stock scanner/scanner/tests/test_scanner_top.py</files>
  <action>
  In run()'s single-profile path, after the composite-desc sort + DataFrame build (~1108),
  truncate to `df.head(top_n)` when top_n is set (the passers array + save_json then reflect
  it). Update the --top help (remove "Ignored for single-profile mode"). Guard: top_n None →
  no truncation (unchanged). Rejections summary counts are over the FULL screened set
  (screened/passed reflect the whole universe; only the picks list is capped). TEST
  test_scanner_top.py: run() (or the truncation helper) with 5 passers + top_n=2 → df has 2
  rows, highest composite; top_n=None → all 5.
  </action>
  <verify>cd "/Users/Ambartsum/code/Stock scanner/scanner" && python3 -m pytest tests/test_scanner_top.py -q && python3 scanner.py --profile quality_moat --universe quality_growth --json /tmp/top.json --top 2 --no-rss-poll >/dev/null 2>&1; python3 -c "import json;print('picks',len(json.load(open('/tmp/top.json'))))"</verify>
  <done>--top truncates single-profile picks by composite; help updated; passed/screened still full-universe; test green.</done>
</task>

<task type="auto">
  <name>Task 3: Surface sensitivity through run_quality_screen (Dexter)</name>
  <files>src/tools/finance/quality-screen.ts, src/tools/finance/quality-screen.test.ts</files>
  <action>
  Extend FailureSummary + normalizeFailureSummary to carry `sensitivity` (defensive coercion;
  never throw). Attach to the tool result. Update RUN_QUALITY_SCREEN_DESCRIPTION: the model
  can answer "what would it take to get more passers?" from sensitivity instead of loosening
  its reasoning. Add unit tests for the sensitivity normalization (non-object/missing → safe).
  Confirm `top` now bounds picks end-to-end (the buildScanArgs --top already flows; the
  scanner now honors it).
  </action>
  <verify>cd /Users/Ambartsum/code/Dexter && bun test src/tools/finance/quality-screen.test.ts 2>&1 | tail -4 && bun run typecheck 2>&1 | tail -2</verify>
  <done>failure_summary carries normalized sensitivity; description documents it; top bounds picks; tests + typecheck green.</done>
</task>

</tasks>

<verification>
**4-reviewer close-out (Workflow, parallel, adversarially verified):**
1. spec-compliance — sensitivity sole-blocker logic; --top truncates by composite; both surfaced.
2. code-quality+security — sensitivity derived from structured results (no re-scan); truncation doesn't corrupt screened/passed counts.
3. silent-failure-hunter — top_n None unchanged; sensitivity empty when no sole-blockers (not a crash); tool never throws.
4. test-thoroughness — sole-blocker counting; top=2 vs None; sensitivity normalization edge cases.

Conditional: type-design N/A. comment-analyzer N/A.

Before complete:
- [ ] sensitivity + --top tests green; full scanner suite green
- [ ] LIVE: --top 2 caps picks to 2; sensitivity['ROIC'] reports sole-blockers on a real scan
- [ ] bun test + typecheck green
- [ ] **Phase 03 complete**
</verification>

<success_criteria>
- gate-sensitivity "what-if" report derived from structured results; surfaced on the tool.
- --top truncates single-profile picks by composite (no longer inert); screened/passed intact.
- Full suite green; tool never throws; Phase 03 done.
</success_criteria>

<output>
Create `.planning/phases/03-screen-accuracy/03-03-SUMMARY.md` (sensitivity shape, --top fix,
live result). Mark Phase 03 complete in STATE.md. Next: Phase 04 (backtest + universe perf).
</output>
