---
phase: 01-failure-observability
plan: 02
type: execute
domain: dexter (TypeScript)
---

<objective>
Have `run_quality_screen` request + read the scanner's `--rejections-json` output and attach
a `failure_summary` to its result, then rewrite the tool description so the model interprets
0-passers by reading the gate tally — never by inventing "backend limitations".

Purpose: This is the fix for the exact live misdiagnosis (0-passers → "backend limitations";
KO D/E hallucinated as 0.00). Depends on Phase 01-01 (which emits the rejections file).
Output: updated `quality-screen.ts` + a unit test for the summary-attach logic.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-failure-observability/01-01-PLAN.md

# Source files (this repo, Dexter):
@src/tools/finance/quality-screen.ts
# @src/tools/finance/types.ts  — formatToolResult
# @src/gateway/config.test.ts  — existing *.test.ts pattern (run via `bun test`)

**Established patterns:** `run_quality_screen` spawns `scanner.py --profile … --json <tmp>`,
drains stdout, reads the tmp JSON file, coerces non-array → [], curates SELECT fields,
ALWAYS returns `formatToolResult` and NEVER throws. Second tmp file is the same pattern.
**Constraining decisions:** D6 (tool never throws — guard missing/non-object rejections
JSON → empty summary), D2/D3 (scanner already emits the summary object; the tool passes it
through, it does NOT re-tally). Test runner is `bun test`.
**Prereq:** 01-01 merged so `scanner.py --rejections-json <path>` writes
`{screened, passed, rejected, gate_tally, samples}`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read the rejections file and attach failure_summary to the result</name>
  <files>src/tools/finance/quality-screen.ts</files>
  <action>
  In `runQualityScreen.func`, alongside `outPath`, create a second temp path
  `rejPath = join(tmpdir(), \`qm_rej_${process.pid}_${Date.now()}.json\`)`. Append
  `'--rejections-json', rejPath` to the `args` array (so both the picks JSON and the
  rejections JSON are produced in one scan).

  After the scan, read+parse rejPath the SAME defensive way as outPath: try/catch around
  `JSON.parse(readFileSync(rejPath,'utf-8'))`, `finally { rmSync(rejPath,{force:true}) }`.
  Then a pure helper (Task 2 tests it) — define and export `normalizeFailureSummary(raw:
  unknown): FailureSummary | undefined`:
    - if raw is not a non-null object → return undefined (D6 guard);
    - else pick {screened, passed, rejected} as finite numbers (default 0), gate_tally as a
      plain record of string→number (drop non-number values), samples as an array (cap to 8,
      each {symbol, failures[]}); return the cleaned object.

  Include it in formatToolResult ONLY when defined:
    formatToolResult({ profile, screened: …, passed: picks.length, picks,
      ...(summary ? { failure_summary: summary } : {}) }, [ …sources… ])
  Keep the existing `if (!r.ok && records.length === 0)` early-return, BUT enrich its error
  payload with the summary too (so a hard failure still shows any partial tally). Never throw.

  Avoid: do not block picks output if rejPath is missing (a full-pass run may still write an
  empty-ish summary); do not let a huge samples array bloat the payload (cap 8, already capped
  scanner-side but re-cap defensively).
  </action>
  <verify>cd /Users/Ambartsum/code/Dexter && bun run build 2>&1 | tail -5   (compiles clean; if no build script, `npx tsc --noEmit` on the file's project)</verify>
  <done>quality-screen.ts compiles; scan spawns with --rejections-json; result includes failure_summary when the scanner reported rejections; tool still returns formatToolResult on every path.</done>
</task>

<task type="auto">
  <name>Task 2: Unit-test normalizeFailureSummary + rewrite the tool description</name>
  <files>src/tools/finance/quality-screen.test.ts, src/tools/finance/quality-screen.ts</files>
  <action>
  Create src/tools/finance/quality-screen.test.ts (bun test; mirror imports/style of
  src/gateway/config.test.ts). Test `normalizeFailureSummary` directly (pure, no spawn):
  1. non-object input (null, undefined, "x", 42, []) → undefined for null/undefined/"x"/42;
     for [] (array is an object) → decide + assert: treat array as invalid → undefined.
  2. valid summary object → returned cleaned, gate_tally preserved, numbers coerced.
  3. gate_tally with a non-number value → that key dropped, others kept.
  4. samples longer than 8 → capped to 8.
  5. missing numeric fields → default to 0 (screened/passed/rejected).

  Then REWRITE two description strings in quality-screen.ts so the model uses the data:
  - In RUN_QUALITY_SCREEN_DESCRIPTION add a "## Interpreting an empty result" section:
    "If `passed` is 0, this is NOT a backend/data failure. Read `failure_summary.gate_tally`
    to state WHY names were rejected (e.g. '19 of 40 failed Debt/Equity < 0.5'), and cite
    `failure_summary.samples` for specific tickers. NEVER attribute 0 passers to 'backend
    limitations' — the scan ran; the names simply did not clear the gates. To find passers,
    widen the universe (see the `universe` argument) rather than loosening your reasoning."
  - In the `.description` one-liner, append: "On 0 passers, returns a failure_summary
    (per-gate tally + sample reasons) explaining why — not an error."
  </action>
  <verify>cd /Users/Ambartsum/code/Dexter && bun test src/tools/finance/quality-screen.test.ts 2>&1 | tail -15</verify>
  <done>All normalizeFailureSummary cases pass under `bun test`; both description strings updated with the "empty result ≠ backend limitation" guidance.</done>
</task>

</tasks>

<verification>

**Validation pattern (4-reviewer close-out, parallel via Agent tool, run_in_background):**
1. spec-compliance (`feature-dev:code-reviewer`) — 5 test cases present; both descriptions updated; done-criteria PASS/FAIL.
2. code-quality (`feature-dev:code-reviewer`) — D6 never-throws preserved on the new read path; temp-file cleanup in finally; payload size cap.
3. silent-failure-hunter (`pr-review-toolkit:silent-failure-hunter`) — a missing/garbage rejections file degrades to `undefined` summary (logged), not a crash and not a false "all passed".
4. test-thoroughness (`pr-review-toolkit:pr-test-analyzer`) — non-object guard, number coercion, sample cap, empty-tally.

Conditional lenses:
- [x] type-design-analyzer applies — this plan introduces a `FailureSummary` type (interface/type alias) in quality-screen.ts. Run `pr-review-toolkit:type-design-analyzer` on that type (invariants: counts ≥ 0, gate_tally values numeric, samples capped).
- [ ] type-design-analyzer does NOT apply.
- [ ] comment-analyzer applies — NO (description strings are user-facing tool copy, not code docstrings; short).
- [x] comment-analyzer does NOT apply.

Before declaring plan complete:
- [ ] `bun test src/tools/finance/quality-screen.test.ts` → all pass
- [ ] `bun run build` (or `npx tsc --noEmit`) clean
- [ ] Manual: a KO/MSFT/NVDA run returns `passed:0` WITH a populated `failure_summary.gate_tally`
- [ ] Description explicitly forbids the "backend limitations" misread
</verification>

<success_criteria>
- run_quality_screen attaches failure_summary (tally + samples) whenever names are rejected.
- Tool description instructs the model to explain 0-passers via the tally, never "backend limitations".
- normalizeFailureSummary unit-tested; tool still never throws; build clean.
- 4-reviewer + type-design-analyzer run at close-out.
- **Phase 01 complete** after this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/01-failure-observability/01-02-SUMMARY.md`
(what shipped, the FailureSummary shape, description-copy diff, next: Phase 02 / 02-01).
Mark Phase 01 complete in STATE.md.
</output>
